============
Installation
============

At the command line::

    $ easy_install django-cache-lock

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-cache-lock
    $ pip install django-cache-lock
